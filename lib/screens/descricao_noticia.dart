import 'package:flutter/material.dart';
class DescricaoNoticia extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Descrição Notícia'),),
      body: Center(
        child: Text('ºhello'),
      ),
    );
  }
}